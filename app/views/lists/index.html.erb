<h1>movie's list</h1>
<ul>
  <% @lists.each do |list| %>
  <li>
  <%= link_to list.name, list_path(list), class: "text-decoration-none" %> 
  </li>
  <% end %>
</ul>

<button type="button" class="btn btn-primary">Add a list
<%= link_to "add a list" , new_list_path(@restaurant) %></button>

<%# <%= cl_image_tag("THE_IMAGE_ID_FROM_LIBRARY",
      width: 400, height: 300, crop: :fill) %>

<!-- for face detection -->
<%# <%= cl_image_tag("IMAGE_WITH_FACE_ID",
      width: 150, height: 150, crop: :thumb, gravity: :face) %>
